batch_size: 128
epochs: 10
noise_std: 0.0 # Gaussian noise std added to inputs during training (0 = disabled)

optimizer:
  _target_: torch.optim.Adam
  lr: 0.001

criterion:
  _target_: torch.nn.CrossEntropyLoss
